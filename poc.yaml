# Test machine
states:
  - name: a
    classname: GameStateA
    connections:
      - expression: "{{val1:Integer}} > 5"
        target: b
      - expression: "{{val2:Boolean}}"
        target: c
  - name: b
    classname: GameStateB
    connections:
      - expression: "{{val3:String}} == \"Ciao\""
        target: c
  - name: c
    classname: GameStateC
